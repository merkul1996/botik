module.exports = {
  PRO_PRICE_STARS: 299,
  FANTASY_PRICE_STARS: 449,
  FREE_DAILY_LIMIT: Number(process.env.FREE_DAILY_LIMIT || 20),
  PRO_DAILY_LIMIT: Number(process.env.PRO_DAILY_LIMIT || 200),
  TRIAL_DAYS: 3,
  PRO_DURATION_DAYS: 30,
  FANTASY_DURATION_DAYS: 30,
  STARS_MIN_TOPUP: 50,
  STARS_MAX_TOPUP: 1000,
  STARS_PRESETS: [50, 100, 250, 500, 1000],
  RATE_LIMIT_WINDOW: 60_000,
  RATE_LIMIT_MAX: 30,
  ADMIN_USER_IDS: (process.env.ADMIN_USER_IDS || "").split(",").map(s => s.trim()).filter(Boolean),
  MAX_MESSAGE_LENGTH: 2000,
  MAX_FACTS_PER_USER: 50,
  MAX_CHAT_HISTORY: 100,
  AI_CHAT_TIMEOUT_MS: Number(process.env.AI_CHAT_TIMEOUT_MS || 75000),
};
